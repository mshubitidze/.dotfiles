git:
  overrideGpg: true
  pagers:
    - colorArg: always
      pager: delta-theme --paging=never
gui:
  nerdFontsVersion: "3"
# gui.theme is layered on from themes/<family>-<light|dark>.yml by the lazygit
# fish wrapper, and in nvim by snacks (see nvim core.lua).
